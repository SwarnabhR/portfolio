// app/page.tsx
//
// HOME PAGE
// ============================================================
// This file is the '/' route — your homepage.
//
// LEARNING — Server Component:
// No 'use client' = renders on the server.
// We'll add sections here one by one as we build them.
// For now it's a blank dark page — that's intentional.
// We're verifying the foundation works before adding content.
// ============================================================

export default function Home() {
  return (
    <main>
      {/* Sections will be added here as we build them */}
      {/* Step 10 → <HeroSection />   */}
      {/* Step 11 → <AboutSection />  */}
      {/* Step 12 → <ServicesSection /> */}
      {/* and so on...                */}
    </main>
  )
}